<script lang="ts">
	import type { Municipality } from "$lib/municipalities";
	import type { Gender } from "$lib/server/db/schema";
	let birthyear: number | undefined = $state(undefined);
	let municipality: Municipality | undefined = $state(undefined);
	let gender: Gender | undefined = $state(undefined);
</script>

<form>
	
</form>
